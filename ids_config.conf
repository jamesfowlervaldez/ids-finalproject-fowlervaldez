# IDS configuration file

# Packet capture duration in seconds
CAPTURE_DURATION=10

# Thresholds for alerts
SYN_THRESHOLD=20       # Max allowed SYN packets in a capture window
HTTP_THRESHOLD=5       # Max allowed HTTP POST requests in a capture window
SSH_THRESHOLD=5        # Max allowed failed SSH logins in last 100 lines of auth.log
